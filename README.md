# BIL476HW
Dependencies:
Step 1. selenium python
  pip install selenium
  geckodriver executable(https://github.com/mozilla/geckodriver/releases) must be in PATH
Step 2. scrapy
  pip install scrapy
3.docker
4.scrapy-splash
   pip install scrapy-splash
   docker run -p 8050:8050 scrapinghub/splash
   splash can be accessed from 0.0.0.0:8050 as default
